# Withings OAuth application credentials.
# Replace with Sealed Secrets / External Secrets in production.
apiVersion: v1
kind: Secret
metadata:
  name: withings-credentials
type: Opaque
stringData:
  WITHINGS_CLIENT_ID: "your-client-id"
  WITHINGS_CLIENT_SECRET: "your-client-secret"
